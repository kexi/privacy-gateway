# Computations

Sanctioned computations (`type: Attested Computation`). A consumer runs the executor,
then the attester, and refuses to present a value whose attestation failed.

- [Leak check on a gateway response](/computations/leak-check.md) — `runtime: typescript`.
  Confirms a core agent's **still-tokenized** response carries no raw PII, before any
  rehydration takes place.
