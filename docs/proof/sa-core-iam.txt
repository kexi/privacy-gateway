# sa-core project IAM roles (all-thinkgs), captured 2026-08-28
# gcloud projects get-iam-policy all-thinkgs --flatten='bindings[].members' \
#   --filter='bindings.members:sa-core@all-thinkgs.iam.gserviceaccount.com' --format='value(bindings.role)'

roles/aiplatform.user
roles/cloudtrace.agent
roles/logging.logWriter

# No roles/datastore.* entry: Core structurally cannot read the Token Vault.
